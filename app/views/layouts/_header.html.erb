<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
       <%= link_to "AFParser", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><a href="<%= movies_path %>">Movie <span class="sr-only">(current)</span></a></li>
        <li class="inactive"><a href="<%= articles_path %>">Article<span class="sr-only">(current)</span></a></li>
      </ul>
     

      <% if current_user %>
      <%= form_tag new_article_path, :method => "get", class: "navbar-form navbar-left" do %>
       <%= text_field_tag :new_article, params[:article_url], class: "form-control", placeholder: "Vocket Url" %>
       <%= submit_tag "Scrape Article", :name => nil, class: "btn btn-default" %>
      <%end%>

        <%= form_tag new_movie_path, :method => "get", class: "navbar-form navbar-left" do %>
          <%= text_field_tag :search, params[:movie_url], class: "form-control", placeholder: "Rotten URL" %>
          <%= submit_tag "Scrape Movie", :name => nil, class: "btn btn-default" %>
          <%= link_to "Or New Movie", new_movie_path, class: "btn btn-default" %>
        <% end %>
      <% end %>
     
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li><%= link_to "Settings", edit_user_registration_path %></li>
          <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        <% else %>
        <li><%= link_to "Login", new_user_session_path %></li>
        <% end %>
      </ul>

    </div><!-- /.navbar-collapse -->

  </div><!-- /.container-fluid -->
</nav>